{
	"name": "tab-tosser",
	"version": "3.3.2",
	"description": "Automatically close tabs that have been ignored for too long. Free yourself of digital clutter.",
	"directories": {
		"lib": "lib",
		"test": "test"
	},
	"scripts": {
		"test": "qunit",
		"build:cp": "rm -rf dist/* && cp LICENSE.txt dist/ && cp src/{manifest.json,icon.svg} dist/ && cp -r src/_locales dist/ && mkdir dist/lib && cp src/lib/{background.html,first-run.html,support.html,options.html,styles.css} dist/lib/ && mkdir dist/vendor && cp src/vendor/* dist/vendor",
		"build:ts": "npx tsc",
		"build": "npm run build:cp && npm run build:ts",
		"release": "npm run build && web-ext build --overwrite-dest --verbose"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/jeremiahlee/tab-tosser.git"
	},
	"author": "Jeremiah Lee",
	"license": "EUPL-1.1",
	"private": true,
	"bugs": {
		"url": "https://github.com/jeremiahlee/tab-tosser/issues"
	},
	"homepage": "https://www.jeremiahlee.com/tab-tosser/",
	"devDependencies": {
		"@types/firefox-webext-browser": "^109.0.0",
		"@types/node": "^22.7.9",
		"autoprefixer": "^10.4.20",
		"jsdom": "^25.0.1",
		"qunit": "^2.22.0",
		"sinon": "^17.0.2",
		"typescript": "^5.6.3",
		"web-ext": "^8.3.0"
	},
	"engines": {
		"node": ">=18.13.0"
	},
	"type": "module",
	"webExt": {
		"sourceDir": "dist/",
		"artifactsDir": "dist-releases/"
	}
}
